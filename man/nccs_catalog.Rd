% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nccs_catalog.R
\name{nccs_catalog}
\alias{nccs_catalog}
\title{List Valid Filter Values}
\usage{
nccs_catalog(
  field = c("ntee_subsector", "ntee_major_group", "exempt_org_type", "state",
    "subsection_code", "foundation_code", "affiliation_code"),
  labels = FALSE
)
}
\arguments{
\item{field}{One of `"ntee_subsector"`, `"ntee_major_group"`,
`"exempt_org_type"`, `"state"`, `"subsection_code"`,
`"foundation_code"`, or `"affiliation_code"`. The last three are
decoder fields backed by the bundled BMF lookups; they are not
currently accepted by `nccs_read()` but are useful for joining
external data.}

\item{labels}{Logical. If `FALSE` (default), returns a character vector of
valid values. For fields where a short code alone is opaque
(`"ntee_subsector"`, `"exempt_org_type"`), each element is an inline
`"CODE - Description"` string; `nccs_read()` accepts these values back
verbatim. If `TRUE`, returns a tibble with at least a `code` and
`description` column, sourced from the internal `.nccs_lookups` bundle
(see `data-raw/build_lookups.R`). For `"state"`, the tibble pairs USPS
abbreviations with state names.}
}
\value{
A character vector (when `labels = FALSE`) or a tibble (when
  `labels = TRUE`).
}
\description{
Returns the set of valid values for filters used in [nccs_read()], or — with
`labels = TRUE` — a tibble of codes alongside their human-readable
definitions sourced from bundled BMF lookup tables. No network access.
}
\examples{
nccs_catalog("ntee_subsector")
nccs_catalog("ntee_subsector", labels = TRUE)
nccs_catalog("state", labels = TRUE)
nccs_catalog("foundation_code", labels = TRUE)

}
